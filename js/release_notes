Liten bot core v1.2
- добавлен механизм обмена сообщениями для модулей через подписки

Global parser 1.0
- первия версия модуля
- регистрация персеров
- привязка парсеров к режиму модуля (обрабатываются только парсеры, которые могут быть в текущем режиме)
- привзяка персеров к подпискам модулей (не обрабатываются персеры, результаты которых не нужны никаким модулям)
- парсинг комнат, мобов, строки состояния

Scenario v1.1
- изменение команды: лит.сц.показ - показывает список команд в сценарии по столбцам
- изменение команды: лит.муд - теперь удаляет мода по номеру в списке
- новая команда: лит.сц.кудал - удаляет команду с определённым номером из сценария
- fix: нумерация в списке команд начинается с 1
- создание списка мобов для сценария
- создание моба для сценария
- удаление моба
- редактирование моба для сценария
- очистка списка мобов сценария
- автоматический сбор мобов в клетке
- работа с таймерами в сценарии
- отдельный метод для изменения статуса моба
- режим агро по белому списку
- режим агро всех в клетке
- обработка агромобов при выполнении сценария
- изменение статуса всех мобов в зоне
- изменение статуса моба по индексу в списке
- после прохождения сценария выдаётся статистика
- откат шага, если была попытка его сделать при текущем бое
- возобновление сценария в случае если моб ушёл с клетки

---------------------------
Liten bot core v1.1
- при работе с параметрами указывается название модуля
- вагончик рефакторинга

Scenario v1.0
- первая версия модуля
- создание / запись сценария
- удаление спанария
- просмотр списка
- сохранение сценариев
- выполнение сценария

Pray bot v1.1
- исправлена опечатка 'Достигнутов'
- добавлен новый параметр 'показ_позитив' - отображает сочетание букв пометивших строку "позитивной". Полезно при анализе, если словарь слишком универсальный.
- при добавлении слова отображается общее количество слов в словаре
- добавлен метод для удаления слова из словаря - лит.мол.уд , ну что б в файлик каждый раз не лезть